name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  # ─── Fast required checks ───
  lint-and-typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run build -w @itsjust/core

      - run: npm run lint

      - run: npm run format:check

      - run: npx tsc --noEmit

      - run: npm test -- --coverage
        env:
          CI: true

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/

  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: npm audit
        run: npm audit --audit-level=moderate
        # Moderate vulnerabilities don't block releases, but are logged.
        # To make blocking: remove continue-on-error and set audit-level=high.
        continue-on-error: true

      - name: Preflight check
        run: node scripts/preflight.mjs

  docs-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check dead links in markdown
        run: |
          # Simple check for dead relative links in markdown files
          # This finds [](...) patterns and checks if the target exists
          node -e "
            const fs = require('fs');
            const path = require('path');
            let errors = 0;
            const mdFiles = fs.readdirSync('.', { recursive: true }).filter((f) => {
              if (typeof f !== 'string' || !f.endsWith('.md')) return false;
              if (f.includes('node_modules/') || f.startsWith('node_modules/')) return false;
              if (f.includes('.next/') || f.startsWith('.next/')) return false;
              if (f.includes('/dist/') || f.startsWith('dist/')) return false;
              return true;
            });
            for (const file of mdFiles) {
              const content = fs.readFileSync(file, 'utf-8');
              const links = [...content.matchAll(/\[([^\]]*)\]\(([^)]+)\)/g)];
              for (const [, , url] of links) {
                if (url.startsWith('http') || url.startsWith('#') || url.startsWith('mailto:')) continue;
                const resolved = path.resolve(path.dirname(file), url.split('#')[0]);
                if (!fs.existsSync(resolved)) {
                  console.error('Dead link in ' + file + ' -> ' + url);
                  errors++;
                }
              }
            }
            if (errors > 0) process.exit(1);
            console.log('All markdown links valid.');
          "

      - name: Check code fence languages
        run: |
          # Ensure fenced code blocks have a language tag
          node -e "
            const fs = require('fs');
            let errors = 0;
            const mdFiles = fs.readdirSync('.', { recursive: true }).filter(f => typeof f === 'string' && f.endsWith('.md'));
            const fence = String.fromCharCode(96).repeat(3);
            for (const file of mdFiles) {
              const content = fs.readFileSync(file, 'utf-8');
              const lines = content.split(/\r?\n/);
              let inCodeBlock = false;
              const bad = [];
              for (let i = 0; i < lines.length; i++) {
                const line = lines[i];
                const trimmed = line.trim();
                if (!trimmed.startsWith(fence)) continue;
                if (!inCodeBlock) {
                  if (trimmed === fence) bad.push(i + 1);
                  inCodeBlock = true;
                } else {
                  inCodeBlock = false;
                }
              }
              if (bad.length > 0) {
                console.error(file + ' has ' + bad.length + ' code fence(s) without language tag');
                errors++;
              }
            }
            if (errors > 0) process.exit(1);
            console.log('All code fences have language tags.');
          "

  # ─── Slow optional checks ───
  build-and-e2e:
    # Decoupled from lint-and-typecheck so build can succeed independently.
    # Lint/typecheck still run in separate fast check.
    # E2E tests are flaky - skip in CI unless explicitly requested.
    # Run locally: `npm run test:e2e`
    # Run in CI: `CI_E2E=1 npm run test:e2e`
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Build
        run: npm run build

      - name: Bundle size check
        run: node scripts/check-bundle-size.mjs

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: E2E tests
        run: npm run test:e2e
        if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.ref == 'refs/heads/main' && !github.actor == 'EntchenEric')
        timeout-minutes: 10
